{
  "name": "growi-news-feed",
  "private": true,
  "description": "News feed distribution for GROWI InAppNotification",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "validate": "ajv validate -s schema/feed.schema.json -d feed.json -c ajv-formats --all-errors && node --experimental-strip-types scripts/check-uniqueness.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "ajv-cli": "^5.0.0",
    "ajv-formats": "^3.0.1",
    "typescript": "^5.7.0"
  }
}
